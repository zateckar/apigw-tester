node_modules/
dist/
data/
*.log
coverage/
.vite/
.tmp-review/

# local secrets — .env.example is the tracked template
.env
.env.*
!.env.example
.kilo/*

# Go loadgen worker — built locally or in CI, never committed
packages/worker/bin/
